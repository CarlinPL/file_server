name: Terraform CI/CD


on:
  workflow_dispatch: # Trigger manual para inicio do workflow
    inputs:
      apply:
        description: 'Executar o terraform apply ?'
        required: true
        default: 'false'
        type: choice
        options: 
          - 'true'
          - 'false'

jobs:
  job1:
    name: Terraform CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0



  